services:
  postgres:
    build:
      context: .
      dockerfile: ./backend/docker/local/postgres/Dockerfile
    ports:
      - "5432:5432"
    volumes:
      - bankfraud_local_db:/var/lib/postgresql/data
    env_file:
      - ./.envs/.env.local
    networks:
      - bankfraud_local_nw

networks:
  bankfraud_local_nw:
    external: true

volumes:
  bankfraud_local_db:
